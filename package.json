{
  "name": "hl-level-monitor",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test packages/core/core.test.ts",
    "monitor": "bun run packages/monitor/index.ts",
    "web": "cd packages/web && bun run dev",
    "check": "bun test packages/core/core.test.ts && bunx tsc --noEmit"
  },
  "workspaces": [
    "packages/web"
  ],
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
